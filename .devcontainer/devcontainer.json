{
    "name": "OSSIM Container",
    "image": "ossim:0.2",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.cpptools",
                "ms-python.python",
                "twxs.cmake"
            ]
        }
    },
    "postCreateCommand": "apt-get install python3 python3-dev python3-shapely python3-pyproj python3-rasterio python3-pip && pip3 install --ignore-installed \"pybind11[global]\" \"numpy<2.0\" opencv-python-headless --break-system-packages",
     "mounts":[
        "source=${localWorkspaceFolder}/data,target=/opt/data/ossim,type=bind"
    ]
}